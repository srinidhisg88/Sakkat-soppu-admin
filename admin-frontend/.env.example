# Local development (uncomment to point to local API)
# VITE_API_BASE_URL=http://localhost:5001/api

# Production/Staging API base (Railway)
# Set this in your hosting provider's env vars (e.g., Netlify → Site settings → Build & deploy → Environment)
VITE_API_BASE_URL=https://sakkat-soppu-be-production.up.railway.app/api
